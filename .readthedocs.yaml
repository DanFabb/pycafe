version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"

python:
  install:
    - method: pip
      path: packages/pycafe
    - method: pip
      path: packages/pycafe-vibro
    - requirements: docs/requirements.txt

sphinx:
  configuration: docs/source/conf.py